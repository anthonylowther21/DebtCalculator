<?xml version="1.0" encoding="UTF-8"?>
<d:MyBasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DebtCalculator.Pages.HomePage"
xmlns:d="clr-namespace:DebtCalculator.Pages">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="New" Command="{Binding SaveFile}" Order="Primary" Priority="0" />
    </ContentPage.ToolbarItems>
  <d:MyBasePage.Content>
    <ListView ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile, Mode=TwoWay}" >
  <ListView.ItemTemplate>
    <DataTemplate>
      <ViewCell>
         <ViewCell.ContextActions>
            <MenuItem Clicked="OnDelete" Command="{Binding DeleteFile}" CommandParameter="{Binding .}"
               Text="Delete" IsDestructive="true"/>
         </ViewCell.ContextActions>
         <StackLayout Padding="15,0">
             <Label Text="{Binding}" />
         </StackLayout>
      </ViewCell>
    </DataTemplate>
  </ListView.ItemTemplate>
    </ListView>
  </d:MyBasePage.Content>
</d:MyBasePage>
